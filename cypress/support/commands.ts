// Project-specific custom Cypress commands would go here. Reusable backend
// stubs live in ./api.ts as plain helpers the specs import directly, which
// keeps each intercept explicit and easy to follow.
export {};
